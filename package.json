{
  "name": "dbu-clearance-system",
  "version": "1.0.0",
  "description": "DBU Clearance Management System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install && cd client && npm install && npm run build",
    "build:client": "cd client && npm run build",
    "build-client": "cd client && npm run build",
    "migrate": "node-pg-migrate"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "body-parser": "^2.2.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "express-session": "^1.18.2",
    "image-size": "^2.0.2",
    "multer": "^2.0.2",
    "node-pg-migrate": "^8.0.4",
    "nodemailer": "^7.0.10",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}